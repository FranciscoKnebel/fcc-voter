<!-- check if user exists. If yes, send userID along with analytics. -->
<% if(typeof user !== "undefined") { %>
	<% include js/analyticsUser %>
<% } else { %>
	<% include js/analyticsNotUser %>
<% } %>
